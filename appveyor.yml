version: '1.1.1-ci.{build}'
init:
  - git config --global core.autocrlf true
branches:
  only:
    - master
build_script:
  - build.cmd
clone_depth: 1
test: off
artifacts:
  - path: 'artifacts\*.nupkg'
    name: nupkg
